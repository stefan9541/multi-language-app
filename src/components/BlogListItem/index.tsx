import React from 'react'
import { BlogItemType } from '../../constants/blogItems'

type Props = {
  item: BlogItemType
}

export default function BlogListItem({item}: Props) {
  return (
    <div>BlogItem</div>
  )
}